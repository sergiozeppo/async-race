const OK = 200;
const CREATED = 201;
const NOT_FOUND = 404;
const ENGINE_BROKE = 500;

export const enum HTTPStatusCode {
  _OK = OK,
  _CREATED = CREATED,
  _NOT_FOUND = NOT_FOUND,
  _ENGINE_BROKE = ENGINE_BROKE,
}
